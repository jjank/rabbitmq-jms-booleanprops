# Boolean properties in rabbitmq-jms

Minimal working example to showcase a problem with boolean properties in `rabbitmq-jms`

## Build and run

The test uses [Testcontainers](https://www.testcontainers.org/) so `docker` is required.